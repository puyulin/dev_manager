(function( factory ) {
	if ( typeof define === "function" && define.amd ) {
		define( ["jquery", "../jquery.validate"], factory );
	} else {
		factory( jQuery );
	}
}(function( $ ) {

/*
 * Translated default messages for the jQuery validation plugin.
 * Locale: ZH (Chinese, 中文 (Zhōngwén), 汉语, 漢語)
 */
$.validator.changeLanguage = function(newLanguage){
	$.validator[newLanguage]();
};
$.validator.zh_CN = function(){
	$.extend($.validator.messages, {
		required: "不能为空",
		//remote: "请修正此栏位",
		email: "电子邮件格式有误",
		url: "网址格式有误",
		date: "日期无效",
		dateISO: "请输入有效的日期 (YYYY-MM-DD)",
		number: "只能输入数字",
		digits: "只可输入整数",
		creditcard: "信用卡号码有误",
		equalTo: "两次输入不一致",
		extension: "后缀名有误",
		maxlength: $.validator.format("最多 {0} 个字符"),
		minlength: $.validator.format("最少 {0} 个字符"),
		rangelength: $.validator.format("长度必须在{0}至{1}之间"),
		range: $.validator.format("长度必须在{0}至{1}之间"),
		max: $.validator.format("必须小于等于 {0}"),
		min: $.validator.format("必须大于等于{0}"),
		maxWords: $.validator.format("最多 {0} 个单词或词组"),
		minWords: $.validator.format("最少 {0} 个单词或词组"),
		rangeWords: $.validator.format("必须在{0}到{1}个单词或词组之间"),
		accept: $.validator.format("请选择正确格式的文件"),
		alphanumeric: $.validator.format("只能是数字字母下划线"),
		currency: $.validator.format("货币类型不正确"),
		extension: $.validator.format("后缀名有误"),
		integer: $.validator.format("必须为整数"),
		ipv4: $.validator.format("IPV4地址不正确"),
		ipv6: $.validator.format("IPV6地址不正确"),
		lettersonly: $.validator.format("只能为a-z的字符串"),
		letterswithbasicpunc: $.validator.format("只能为a-z的字符串（可带基本标点符号）"),
		nowhitespace: $.validator.format("不能有空白"),
		pattern: $.validator.format("格式有误"),
		time: $.validator.format("时间格式应为（hh:mm）"),
		time12h: $.validator.format("时间格式应为（hh:mm A/PM）"),
		phone:$.validator.format("必须为有效的手机号"),
		isMobile:$.validator.format("请正确填写您的手机号码"),
		checkStr:$.validator.format( "只能输入英文"),
		checkCN:$.validator.format("只能输入中文"),
		checkCNname:$.validator.format("名称只能由字母、数字、下划线及中文组成"),
		checkName:$.validator.format("名称只能由字母、数字、下划线、英文中划线及小数点组成"),
		checkIp:$.validator.format("IP地址的格式不合法"),
		"ip":$.validator.format("请填写正确的IP地址"),
		"CN_EN":$.validator.format("只能是字母，中文，数字，下划线和小数点组成"),
		"ltIp":$.validator.format("请注意ip地址范围"),
		'timeBefore':$.validator.format("填写有误"),
		"ltNum":$.validator.format("请注意数字范围"),
		"ip_port":$.validator.format("ip端口地址有误"),
	});
};

$.validator.en_US = function(){
	$.extend($.validator.messages, {
		required: "Mandatory",
		//remote: "请修正此栏位",
		email: "电子邮件格式有误",
		url: "网址格式有误",
		date: "日期无效",
		dateISO: "请输入有效的日期 (YYYY-MM-DD)",
		number: "只能输入数字",
		digits: "只可输入整数",
		creditcard: "信用卡号码有误",
		equalTo: "你的输入不相同",
		extension: "后缀名有误",
		maxlength: $.validator.format("最多 {0} 个字符"),
		minlength: $.validator.format("最少 {0} 个字符"),
		rangelength: $.validator.format("长度必须在{0}至{1}之间"),
		range: $.validator.format("长度必须在{0}至{1}之间"),
		max: $.validator.format("必须小于等于 {0}"),
		min: $.validator.format("必须大于等于{0}"),
		maxWords: $.validator.format("最多 {0} 个单词或词组"),
		minWords: $.validator.format("最少 {0} 个单词或词组"),
		rangeWords: $.validator.format("必须在{0}到{1}个单词或词组之间"),
		accept: $.validator.format("请选择正确格式的文件"),
		alphanumeric: $.validator.format("只能是数字字母下划线"),
		currency: $.validator.format("货币类型不正确"),
		extension: $.validator.format("后缀名有误"),
		integer: $.validator.format("必须为整数"),
		ipv4: $.validator.format("IPV4地址不正确"),
		ipv6: $.validator.format("IPV6地址不正确"),
		lettersonly: $.validator.format("只能为a-z的字符串"),
		letterswithbasicpunc: $.validator.format("只能为a-z的字符串（可带基本标点符号）"),
		nowhitespace: $.validator.format("不能有空白"),
		pattern: $.validator.format("格式有误"),
		time: $.validator.format("时间格式应为（hh:mm）"),
		time12h: $.validator.format("时间格式应为（hh:mm A/PM）"),
		phone:$.validator.format("必须为有效的手机号"),
		isMobile:$.validator.format("请正确填写您的手机号码"),
		checkStr:$.validator.format( "只能输入英文"),
		checkCN:$.validator.format("只能输入中文"),
		checkCNname:$.validator.format("名称只能由字母、数字、下划线及中文组成"),
		checkName:$.validator.format("名称只能由字母、数字、下划线、英文中划线及小数点组成"),
		checkIp:$.validator.format("IP is wrong"),
		"ip":$.validator.format("请填写正确的IP地址"),
		"CN_EN":$.validator.format("只能是字母，中文，数字，下划线和小数点组成"),
		"ltIp":$.validator.format("请注意ip地址范围"),
		'timeBefore':$.validator.format("Error time range"),
		"ltNum":$.validator.format("请注意数字范围")
	});
};
}));